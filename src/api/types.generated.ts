/**
 * 백엔드 OpenAPI(Swagger) 스키마에서 자동 생성되는 타입.
 * 실제 생성: `SWAGGER_URL=https://api.example.com/openapi.json npm run api:sync`
 * 현재는 백엔드 스펙 미확정으로 비어 있는 placeholder 다. (수동 편집 금지 — 생성물)
 */
export type paths = Record<string, never>
export type components = Record<string, never>
export type operations = Record<string, never>
