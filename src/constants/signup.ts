import type { SelectOption } from '@types'

// 기수·파트 선택지. 디자인에 목록이 없고 백엔드도 연동 전이라 비워 둔다 (연동 시 API 값으로 채운다).
export const COHORT_OPTIONS: SelectOption[] = []
export const PART_OPTIONS: SelectOption[] = []
