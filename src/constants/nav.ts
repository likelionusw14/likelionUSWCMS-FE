import type { NavItem } from '@types'

// 관리자(운영진) 영역 네비.
export const ADMIN_NAV: NavItem[] = [{ to: '/admin', label: '대시보드' }]

// 사용자(아기사자) 영역 네비.
export const USER_NAV: NavItem[] = [{ to: '/app', label: '홈' }]
