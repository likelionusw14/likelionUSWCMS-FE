# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# TypeScript
*.tsbuildinfo

# Env (예시 파일 .env.example 만 추적)
.env
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 가역 삭제 trash — rm 대신 여기로 mv. 주기적으로 정리.
.trash/

# Figma 디자인 참고용 임시 스크린샷 (로컬 전용, 커밋 제외)
.figma-inspect/

# 로컬 전용 컴포넌트 미리보기(sandbox) — git 미추적, 절대 커밋/배포 금지. App.tsx 가 있을 때만 로드.
src/routes/devRoutes.local.tsx
src/routes/SandboxPage.tsx
src/routes/Sandbox2Page.tsx

# Claude Code — 팀 공유 설정(.claude/commands, settings.json)은 커밋, 개인/로컬만 무시
.claude/settings.local.json
.claude/*.local.json

# 로컬 API 스펙 스크래치 파일 (백엔드 OpenAPI 원본은 openapi/ 에서 관리)
cms-api-*.yaml
