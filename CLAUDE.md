# CLAUDE.md

이 저장소의 컨벤션은 [`AGENTS.md`](./AGENTS.md) 를 **단일 출처**로 한다 (Claude/Codex 공용). 아래 import 로 전체 내용을 불러온다 — 컨벤션 수정은 `AGENTS.md` 에서 한다.

@AGENTS.md

## Claude 전용 메모

- 프로젝트 슬래시 명령은 `.claude/commands/` 에 있다: `/commit`, `/pr`, `/scaffold-component`, `/figma-import`.
- 같은 명령의 Codex 버전은 `.codex/prompts/` 에 미러돼 있다 (내용 동일).
